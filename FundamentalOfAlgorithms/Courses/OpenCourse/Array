数组：
	数组下标是一种特殊的hash
	理解数组与map
	给数组顺序
查找和排序：
	二分查找
	元素交换
	排序，中位数
	归并
	位运算
	前缀和应用
动态规划
排列组合

例题：
	1、给定一个不包含相同元素的整数数组，求局部最小值。（定义为比邻居小的值）
		使用二分法，来寻找极小值
		leetcode,153,154
		严格递增寻找是否存在a[x]=x
	2、给一个数组，找到从1开始第一个不在里面的正整数
	3、给定一个整数数组(n>1),把这些数放在数轴上，相邻两个数差的最大值
		放在n+1个桶里
	4、给定一个数组，除了有两个只出现一次的数，剩下的元素都出现了两次，求这两个数
		使用异或
		leetcodt137
		1-100缺了两个数，求这两个数
	5、众数问题
		找出占比超过一半的数
		找出多于k-1的数
	6、给定浮点数组a，求一个数组b，b[i]=a[0]*a[1]*...a[i-1]*a[i+1]*...*a[n-1],不能用除法，不能再开数组
		for一次后缀，再for一次前缀
		前缀和的性质
			a[i]+a[i+1]+...+a[j]=sum[j]-sum[i-1];
		思考
			求数组中连续一段和，绝对值最小
总结：
	利用序
		二分查找
	利用前缀和
		查找，计算，排序
	理解数组
		map
	用数组实现数据结构
		一般树：村每个阶段的父亲
		二茶数：堆得实现
	抓住简单题：
		分治算法求逆序对数
		有序数组归并
		两个有序数组的中位数
		两头扫的方法